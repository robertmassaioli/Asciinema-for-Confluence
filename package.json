{
  "name": "asciinema-for-confluence",
  "version": "1.0.0",
  "description": "Forge app that embeds asciinema terminal recordings in Confluence via three macros and a Smart Link resolver.",
  "main": "src/index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "cd static/app && npm install && npm run build"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/react": "^11.0.0",
    "@forge/resolver": "1.7.1"
  }
}
